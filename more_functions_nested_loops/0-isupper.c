#include "main.h"
/**
* _isupper - will check if its uppercase
* @c: the variable of the code
* Return: will come out to 0
**/
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	return (1);

	else
	return (0);
}
