#include <stdio.h>
#include <string.h>

int main () {
	// using the puts function
   char str1[55];

   strcpy(str1, "Programming is like building a multilingual puzzle\n");

   puts(str1);
   
   return(0);
}
